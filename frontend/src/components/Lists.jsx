import React from "react";
/* import {UseState, useEffect} from "react"; 
 import axios from "axios"; */

function Lists() {
  /* title, fetchUrl */
  return <div>Lists</div>;
}

/* A mettre sur les miniatures : <Link to={`player/${movie?.id}`}> */

export default Lists;
